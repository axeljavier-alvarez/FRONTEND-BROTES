import { BuscarplantapipePipe } from './buscarplantapipe.pipe';

describe('BuscarplantapipePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarplantapipePipe();
    expect(pipe).toBeTruthy();
  });
});
